name: Main

on:
  workflow_call:
  workflow_dispatch:

jobs:
  call-one:
    name: CallerOne
    if: false
    uses: ./.github/workflows/lvl3.yml

  call-two:
    name: Caller Two
    if: false
    uses: ./.github/workflows/lvl3.yml

  call-skip:
    name: Skipper
    if: false
    uses: ./.github/workflows/lvl3.yml
