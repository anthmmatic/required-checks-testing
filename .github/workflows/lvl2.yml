name: Main

on:
  workflow_call:
  workflow_dispatch:

jobs:
  call-one:
    name: CallerOne
    uses: ./.github/workflows/lvl3.yml

  call-two:
    name: Caller Two
    uses: ./.github/workflows/lvl3.yml

  call-skip:
    name: Skipper
    uses: ./.github/workflows/lvl3.yml
